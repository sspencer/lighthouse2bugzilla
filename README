These scripts work in conjunction to move export bugs from LightHouse and import them into Bugzilla.
The export script creates a JSON file.  The import script creates a SQL file.

Much has to be customized with the scripts.

Good luck.